create database `naranti`;
#A linha acima serve para criar um banco de dados com o nome: `naranti`
#O uso da crase se torna necessaio para indicar uma `string` do sistema
# `strings` do sistema: `database`, `table` e `campo`